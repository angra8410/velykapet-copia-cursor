# Variables de entorno para DESARROLLO

# Configuración del servidor frontend
FRONTEND_PORT=3333
FRONTEND_HOST=localhost

# Configuración del servidor backend
# IMPORTANTE: Este puerto debe coincidir con la configuración en:
# backend-config/appsettings.Development.json -> Kestrel.Endpoints.Http.Url
# El backend por defecto escucha en http://localhost:5135 (HTTP, no HTTPS)
BACKEND_PORT=5135
BACKEND_HOST=localhost

# Configuración de la aplicación
APP_ENV=development
APP_DEBUG=true

# URLs
# IMPORTANTE: Usar HTTP (no HTTPS) en desarrollo para evitar problemas con certificados
# Si el backend está configurado para HTTPS, cambiar a https://localhost:5135
# Ver PORT_CONFIGURATION.md para más información
API_URL=http://localhost:5135
FRONTEND_URL=http://localhost:3333