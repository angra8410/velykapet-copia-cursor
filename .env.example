# Variables de entorno por defecto
# Este archivo se usa cuando no hay un ambiente específico configurado

# Configuración del servidor frontend
FRONTEND_PORT=3333
FRONTEND_HOST=localhost

# Configuración del servidor backend
# IMPORTANTE: El backend en desarrollo escucha en http://localhost:5135 (sin HTTPS)
# Si experimentas ERR_CONNECTION_REFUSED, verifica que:
# 1. El backend esté corriendo (dotnet run)
# 2. El puerto 5135 no esté ocupado
# 3. Estés usando HTTP (no HTTPS) en desarrollo local
BACKEND_PORT=5135
BACKEND_HOST=localhost

# Configuración de la aplicación
APP_ENV=development
APP_DEBUG=true

# URLs
# Para desarrollo local, usa HTTP en el puerto 5135
# El proxy del frontend (puerto 3333) redirige las peticiones al backend
API_URL=http://localhost:5135
FRONTEND_URL=http://localhost:3333
